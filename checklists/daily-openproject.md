# Checklist Diario - OpenProject

Data:
Responsavel:

- [ ] `docker ps` mostra `openproject-web` e `openproject-db` em `Up`
- [ ] `curl -I http://127.0.0.1:8080` responde
- [ ] login web testado
- [ ] logs recentes sem erro repetitivo
- [ ] disco (`/`, `/var`, `/srv/data`) abaixo do limite
- [ ] backup (`op-backup`) executou com sucesso
- [ ] registrar anomalias em `openproject/incidentes/`

Observacoes:

